using UnityEngine;

namespace  SoulEngine.User_Interface.Level_Selection
{
	public class LevelSelectionUIController : MonoBehaviour
	{
		/*
		 * When user clicks on level
		 * pass scriptableobject containing level information to ui manager
		 * manager loads a menu and passes the level data to it
		 * level ui menu populates itself with the data
		 * player clicks play and it begins to load the given level.
		 */
	}
}
